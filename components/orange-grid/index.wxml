<view class="container">
    <l-grid l-class="inner-container" row-num="5">
        <block wx:for="{{grid}}">
            <l-grid-item l-class="l-grid-item" key="{{index}}" slot="{{index}}">
                <view class="grid-item">
                    <image class="img" src="{{item.img}}"></image>
                    <text class="text">{{item.title}}</text>
                </view>
            </l-grid-item>
        </block>
    </l-grid>
</view>